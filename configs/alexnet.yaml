experiment:
  name: "alexnet"
  seed: 42
  device: "cuda"

model:
  name: "alexnet"
  in_channels: 3
  num_classes: 10

dataset:
  name: "EuroSAT"
  image_size: 224
  batch_size: 128

training:
  epochs: 30
  optimizer:
    name: "adam"
    lr: 0.0001
    weight_decay: 0.0001
