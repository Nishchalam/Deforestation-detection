experiment:
  name: "vgg16"
  seed: 42
  device: "cuda"

model:
  name: "vgg16"
  in_channels: 3
  num_classes: 10

dataset:
  name: "EuroSAT"
  image_size: 224
  batch_size: 64

training:
  epochs: 30
  optimizer:
    name: "adam"
    lr: 0.0001
    weight_decay: 0.0001
